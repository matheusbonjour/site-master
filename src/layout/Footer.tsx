import { Box } from "@mui/material";

const Footer: React.FC = () => {
  return (
    <Box
      sx={{
        backgroundColor: "#333333",
        height: "200vh",
        position: "flex",
      }}
    ></Box>
  );
};

export default Footer;
