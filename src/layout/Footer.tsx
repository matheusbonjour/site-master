import { Box } from "@mui/material";

const Footer: React.FC = () => {
  return (
    <Box
      sx={{
        backgroundColor: "#333333",
        height: "20vh",
        position: "flex",
      }}
    ></Box>
  );
};

export default Footer;
